---
    - name: Deploy to Kubernetes
      hosts: localhost 
      connection: local
     
      tasks: 
      - name: create a deployment
        command: /usr/bin/local/kubectl apply -f deployment.yaml
     
      - name: create a service
        command: /usr/bin/local/kubectl apply -f service.yaml