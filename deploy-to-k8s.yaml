---
    - name: Deploy to Kubernetes
      hosts: localhost 
      connection: local
     
      tasks: 
      - name: create a deployment
        command: /usr/local/bin/kubectl apply -f deployment.yaml
     
      - name: create a service
        command: /usr/bin/local/bin/kubectl apply -f service.yaml